function [win,lose]=compare(a,b)
%COMPARE 此处显示有关此函数的摘要
%   此处显示详细说明
        a=pop(p_new(run_time));
        b=pop(p_new(run_time+1));
 if dominates(a.cost(),b.cost())
     win=a;
     lose=b;
 else
     win=b;
     lose=a;
 end 
end

